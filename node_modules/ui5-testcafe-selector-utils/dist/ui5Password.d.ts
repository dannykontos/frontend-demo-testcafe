declare class ui5PassworDef {
    private _secretKey;
    private _iv;
    constructor();
    decrypt(hash: string): string;
    encrypt(text: string): string;
}
export declare let ui5Password: ui5PassworDef;
export {};
