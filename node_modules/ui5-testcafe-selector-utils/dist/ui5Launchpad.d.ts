export interface ui5LaunchpadStartupParams {
    user: string;
    password: string;
    afterLogin?: () => Promise<any>;
    tile?: string;
}
declare class ui5LaunchpadDef {
    startup(params: ui5LaunchpadStartupParams): Promise<void>;
    login(userName: string, password: string): Promise<void>;
    openTile(sHash: string): Promise<void>;
}
export declare var ui5Launchpad: ui5LaunchpadDef;
export {};
