name: End-to-End Tests
on: [push]
jobs:
  test:
    name: Run TestCafe Tests
    runs-on: ubuntu-latest
    steps:
      - run: ls -la
      - name: Check out the repository
        uses: actions/checkout@v1
      - run: cd /home/runner/work/frontend-demo-testcafe/frontend-demo-testcafe
      - run: ls -la
